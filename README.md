📚 Library Management System
Простое и эффективное Spring Boot приложение для управления библиотекой: книгами, журналами и участниками. Проект построен с использованием многослойной архитектуры для чистоты кода и легкости масштабирования.

🏗 Архитектура проекта
Проект разделен на логические слои (пакеты), что соответствует принципам Clean Architecture:

config: Содержит настройки подключения к базе данных (DbConfig).

controllers: Обрабатывает входящие HTTP-запросы и возвращает ответы пользователю.

repository: Содержит логику прямого взаимодействия с PostgreSQL через JDBC (SQL-запросы).

entities: Описывает модели данных (Book, Magazine, Member), которые хранятся в базе.

🛠 Технологии
Java 17+

Spring Boot (Web, Thymeleaf)

PostgreSQL (База данных)

JDBC (Для работы с БД)

Maven (Сборка проекта)

🚀 Как запустить
Настройте базу данных:

Убедитесь, что у вас установлен PostgreSQL.

Создайте базу данных (по умолчанию postgres).

Выполните SQL-скрипты для создания таблиц book, magazine и librarymember.

Конфигурация:

Проверьте настройки доступа в файле src/main/java/kz/aitu/Library/config/DbConfig.java.

Укажите ваш актуальный URL, USER и PASS.

Запуск:

Запустите класс LibraryApplication.

📍 Основные API Endpoints
Книги (/api/books)

GET /getBooks — получить список всех книг.

POST /addBooks — добавить новые книги.

DELETE /deleteBook — удалить книгу по названию.

Журналы (/getMagazines)

GET /getMagazines — список всех журналов.

POST /addMagazines — пакетное добавление журналов.

Участники (/getMembers)

GET /getMembers — список всех зарегистрированных участников.

POST /addLibraryMembers — добавление новых участников.

📂 Структура папок в IDE
Plaintext
src/main/java/kz/aitu/Library/
├── config/         # Конфигурация БД
├── Controllers/    # REST и Web контроллеры  
├── entities/       # Модели данных (POJO)  
├── repository/     # DAO слой (SQL логика) 
└── LibraryApplication.java